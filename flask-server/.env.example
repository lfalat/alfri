# Flask ML Service Environment Configuration

# Database Configuration (Spring Boot format)
DATABASE_DEV_URL=localhost:5432/alfri
DATABASE_DEV_USER=alfri
DATABASE_DEV_PASSWORD=changeme

# API Security
# API_KEY=your-api-key-here
# API_KEYS=key1,key2,key3  # Comma-separated list of allowed keys

# Application Mode
MODE=dev
LOG_LEVEL=INFO

# Model Configuration (optional - uses defaults if not set)
# MODEL_DIR=/path/to/models
# MODEL_MAP={"model_name": {"type": "keras", "path": "model.h5"}}

